# Agents Admin API Server 环境变量
# 此文件由 systemd EnvironmentFile 加载，升级时不会被覆盖
# 用于存放敏感配置（密码、密钥），不应提交到版本控制

# 数据库密码（被 api-server.yaml 中的 database 配置引用）
DB_PASSWORD=CHANGE_ME

# 数据库连接（完整 URL，优先级高于 yaml 配置）
DATABASE_URL=postgres://agents:${DB_PASSWORD}@localhost:5432/agents_admin?sslmode=disable

# Redis 连接
REDIS_URL=redis://localhost:6379/0

# JWT 密钥（认证功能必须，请替换为随机 32+ 字符串）
# 生成方法: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_TO_A_RANDOM_SECRET

# 默认管理员（首次启动自动创建）
ADMIN_EMAIL=admin@your-domain.com
ADMIN_PASSWORD=CHANGE_ME

# 日志级别 (debug, info, warn, error)
LOG_LEVEL=info
