# Agents Admin Node Manager 配置
# 此文件由 systemd EnvironmentFile 加载，升级时不会被覆盖

# API Server 地址
API_SERVER_URL=http://localhost:8080

# 节点 ID（每台机器唯一）
NODE_ID=node-01

# Agent 工作空间目录
WORKSPACE_DIR=/var/lib/agents-admin/workspaces

# 日志级别 (debug, info, warn, error)
LOG_LEVEL=info
