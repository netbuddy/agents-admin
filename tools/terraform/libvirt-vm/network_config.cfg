version: 2
ethernets:
  eth0:
    match:
      name: "en*"
    dhcp4: false
    addresses:
      - ${vm_ip}
    routes:
      - to: default
        via: ${vm_gateway}
    nameservers:
      addresses:
%{ for dns in vm_dns ~}
        - ${dns}
%{ endfor ~}
